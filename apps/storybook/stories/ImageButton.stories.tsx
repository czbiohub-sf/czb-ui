import React from "react";

import { ImageButton as CZBUIImageButton } from "@czb-ui/core/src";

export default {
  title: "Image Button",
  component: CZBUIImageButton,
  argTypes: {},
};

const Template = (args: any) => <CZBUIImageButton />;

export const ImageButton = Template.bind({});
ImageButton.args = {};
